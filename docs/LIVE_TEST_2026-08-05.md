# 实测记录(2026-08-05)

目的:H0→H1→四轮评审修复之后,用同一份证据实测「改了这么多版本,到底改了什么」。
全部零 API 重算(第五节除外,那节是真 API)。代码 HEAD:`6922b51`。

## 一、预注册判定零漂移(留出集 100 份,同输入新 run)

`runs/heldout`(8/2,v1 代码)vs `runs/heldout-r2`(今天代码),同 100 份文档、
同存盘响应、同 OCR:

| H 判据 | 旧 | 新 | 预注册线 | 判定 |
|---|---|---|---|---|
| H1 分诊 lift | 3.037× | 3.037× | > 1.5 | PASS |
| H2 coverage@46% | 74.3% | 74.3% | > 55% | PASS |
| H3 复核召回 | 72.5% | 72.5% | > 55% | PASS |
| H4 缺值率 | 27.9% | 27.9% | 10–45% | PASS |
| H5 citation 失败率 | 14.4% | 14.4% | < 15% | PASS |
| H6 冻结拒绝率 | 34.6% | 34.6% | 5–35% | PASS |

矩阵汇总唯一差别:`requires_adjudication` 610 → 612(+2,即第二节的 C8 两行)。
findings 按门禁:arithmetic 40→39(C3 修复),citation 58→58,cross_mode 255→255,
extraction_present 279→279,wellformed 19→19,cross_document_duplicate 0→2。

复算:`python3 scripts/heldout_metrics.py runs/heldout-r2 runs/heldout`
(需 `INVOICELOOP_CORPUS=runs/heldout-workspace`)。

## 二、语义差异:两处,都在真实数据上抓到东西

1. **C3 日期门修复消除真误报**:`720b8b2e` issue 05/26/1999 → due 06/05/1999
   (美式月在前,完全合法),旧反序比较误判反序,新代码 pass。
   逐槽裁决差异全表只有 4 处:C3 两条(fail→pass)+ C8 两条(新增 fail)。
2. **C8 跨文档查重首战即中**:`6fe42d84` 与 `f0d621ee` 同为
   发票号 #18N0039311 / MORGAN COUNTY INDUSTRIES,INC.,gross 同 $100.00,
   但开票日期一有一无 → 内容冲突,两份的 invoice_number 行进复核队列。
   这是旧代码零检出的类别。

## 三、三方基线(新 run 上重算)

`python3 scripts/baseline_comparison.py runs/heldout-r2 runs/heldout-workspace`

TIER1 关键字段(287 槽):

| 系统 | 自动放行覆盖 | 字段静默错误率 | 文档静默失败率 | 复核负载 | 偏差路由召回 |
|---|---|---|---|---|---|
| raw DWS(全信) | 100% | 29.97% | 54.5% | 0% | 0% |
| 双模式一致 | 74.6% | 14.95% | 31.5% | 25.4% | 62.8% |
| InvoiceLoop 分诊 | 58.2% | 8.98% | 22.2% | 41.8% | 82.6% |

与 `docs/BASELINE_COMPARISON.md`(旧 run)比:静默错误 8.88%→8.98%、覆盖
58.9%→58.2% —— C8 把两个关键字段槽送入复核的代价,其余不变。

## 四、demo 全链路 + 交付层(P2 实战)

`runs/demo-live-2026-08-05/runs/run-0001`(3 份 vendored DocILE 样本):

- 零裁决时 deliverable:3 份全 `pending`(如实);
- 编程式复核:26 条裁决(有值 accept、无值 abstain)→ 仍全 pending
  (abstain=未决,不许带着放行 —— 设计行为);
- 12 条显式 supersession(弃权改判「接受缺失」)→ **3 份全 released**;
- 046e0c49(OCR 受阻)放行带 `release_caveats: ["independent_ocr"]` ——
  机检没跑过的披露随交付物走(本轮实测抓出并修复的缺陷,`6922b51`);
- bundle:三层(members/snapshot/binding)全过;
  sha256 `ffaae2b3bdc31cc9e783015f28bf2d134831e855e90bd91848f6b46da01f96a3`;
- 篡改对照:翻 deliverable.json 压缩流一个字节 → `ok:false`,
  members 层报「成员损坏」,snapshot/binding 仍 true(分层精确)。

## 五、活 DWS(真 API,7 次调用)

`runs/live-dws-2026-08-05`,3 份样本 PDF 现场抽取(`DWS_API_KEY` 环境变量,
不落盘)。credit 实况:key#1 余额 0(全 402)、key#2 完成 1 次后耗尽、
key#3 完成其余 5 次;402 响应存盘照留、ingest 摘要如实计失败、删非 200
存盘后续跑补全 —— 失败路径全部按设计工作。

**同一批 PDF,八月存盘 vs 今天现抽:30 个字段槽漂移 9 个。** 亮点:

- `003cc916`:八月把 `58-0391492` 放进 invoice_number;今天的 understand 把它
  放进 seller_vat_id(冻结绑定通过 —— 页面上确实是它),而 agentic 抽成
  `58-0391482`(**末位错一位,冻结绑定当场拒绝**);agentic 另给
  invoice_number 草稿 `83519`,同样绑定拒绝;
- `046e0c49`(退化扫描件):八月 understand 报 total_gross $120.00,
  今天报空 —— 缺值进队列,不编造;
- 指纹换代演示:在 402 存盘状态下误跑的 run-0001 与补齐后的 run-0002 指纹不同,
  系统没复用坏 run,自动开新一代。

## 环境与复算

- macOS(Darwin 24.5.0),Python 3.14,poppler(pdftotext/pdftoppm/pdfinfo)+
  tesseract 在场;测试 `python3 -m pytest tests/` = **300 passed**;
  `scripts/fresh_venv_check.sh`(干净 venv 评委门)= 259 passed + 41 skipped(语料守卫)。
- 工件:`runs/heldout-r2`(新代码留出集 run)、`runs/demo-live-2026-08-05`、
  `runs/live-dws-2026-08-05` —— 不进 git(runs/ 惯例),在本机盘上。
- 注意:`runs/heldout` 是 v1 形态(无 review_snapshot),verify 只有成员级 ——
  如实标注,不是缺陷;`runs/heldout-r2` 是全 v2 工件。

## 诚实边界(与实测同屏)

- 第一/三节数字是 100 份 DocILE 留出集上的观测值,不主张跨数据集成立(§8 限定三);
- 基线表是探索性分析(2026-08-04/05),非预注册;InvoiceLoop 产品不自动放行,
  表中「放行」是反事实口径;
- 第五节样本量 3 份,只证明活链路工作,不证明任何率。
